services:
  mirofish:
    build: .
    container_name: mirofish-offline
    env_file:
      - .env
    ports:
      - "5001:5001"
    restart: unless-stopped
    volumes:
      - ./backend/uploads:/app/backend/uploads
